{
  "name": "pluginlibrary",
  "version": "0.5.6",
  "description": "Zere's library for BetterDiscord plugins.",
  "dependencies": {},
  "devDependencies": {
    "jsdoc": "^3.5.5",
    "raw-loader": "^0.5.1",
    "webpack": "^4.8.3",
    "webpack-cli": "^2.1.3"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack --config webpack.config.js --progress --colors",
    "build_plugin": "webpack src/plugin.js -o release/0PluginLibrary.plugin.js --config webpack.config.js --progress --colors",
    "build_prod": "webpack --mode production --devtool source-map --config webpack.config.js --progress --colors && jsdoc -r src -c jsdoc.config.json",
    "build_docs": "jsdoc -r src -c jsdoc.config.json"
  },
  "author": "Zerebos",
  "license": "MIT"
}
